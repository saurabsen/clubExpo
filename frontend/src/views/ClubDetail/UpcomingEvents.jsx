import { Typography } from '@mui/material';

const UpcomingEvents = (props) => {
  return (<>
      <Typography variant="h5" component="h5" sx={{pb:1}}>Upcoming Events</Typography>
  </>);
}

export default UpcomingEvents;

